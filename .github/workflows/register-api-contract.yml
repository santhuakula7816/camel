# # Copy the corrected workflow I provided above
# name: Register JSON Map Service API

# on:
#   push:
#     branches:
#       - main
#       - dev
#       - 'release/**'
#     tags:
#       - 'v*'

# jobs:
#   register:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3

#       - name: Determine environment and version
#         id: version
#         run: |
#           if [[ "${{ github.ref }}" == refs/tags/v* ]]; then
#             VERSION="${{ github.ref }}"
#             VERSION=${VERSION#refs/tags/}
#             ENVIRONMENT="prod"
#           elif [[ "${{ github.ref }}" == refs/heads/release/* ]]; then
#             ENVIRONMENT="uat"
#             VERSION="${{ github.ref }}"
#             VERSION=${VERSION#refs/heads/release/}
#           elif [[ "${{ github.ref }}" == refs/heads/main ]]; then
#             ENVIRONMENT="staging"
#             VERSION="main-$(date +%s)"
#           elif [[ "${{ github.ref }}" == refs/heads/dev ]]; then
#             ENVIRONMENT="dev"
#             VERSION="dev-$(date +%s)"
#           else
#             ENVIRONMENT="dev"
#             VERSION="pr-${{ github.event.pull_request.number }}"
#           fi

#           echo "environment=$ENVIRONMENT" >> $GITHUB_OUTPUT
#           echo "version=$VERSION" >> $GITHUB_OUTPUT

#       - name: Verify Postman collection exists
#         run: |
#           if [ ! -f "postman/jsonmap.postman_collection.json" ]; then
#             echo "❌ ERROR: Postman collection not found at postman/jsonmap.postman_collection.json"
#             exit 1
#           fi

#       - name: Register API with Central Registry
#         run: |
#           curl -X POST "${{ secrets.CENTRAL_REGISTRY_URL }}/registry/apis/register-file" \
#             -F "apiName=jsonmap-service" \
#             -F "description=JSON Mapping Service" \
#             -F "gitRepository=${{ github.repository }}" \
#             -F "version=${{ steps.version.outputs.version }}" \
#             -F "breakingChange=false" \
#             -F "collectionFile=@postman/jsonmap.postman_collection.json;type=application/json"
#       - name: Register environment
#         run: |
#           curl -X POST "${{ secrets.CENTRAL_REGISTRY_URL }}/registry/environments" \
#             -H "Content-Type: application/json" \
#             -d '{
#               "envName": "${{ steps.version.outputs.environment }}",
#               "baseUrl": "${{ secrets.SERVICE_BASE_URL }}"
#             }' || true

#       - name: Mark API Deployment
#         run: |
#           curl -X POST "${{ secrets.CENTRAL_REGISTRY_URL }}/registry/apis/deploy" \
#             -H "Content-Type: application/json" \
#             -d '{
#               "apiName": "jsonmap-service",
#               "version": "${{ steps.version.outputs.version }}",
#               "environment": "${{ steps.version.outputs.environment }}",
#               "baseUrl": "${{ secrets.SERVICE_BASE_URL }}"
#             }'

# name: Register JSON Map Service API

# on:
#   push:
#     branches: [main, dev, "release/**"]
#     tags: ["v*"]

# jobs:
#   register:
#     runs-on: ubuntu-latest

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v3

#       - name: Determine environment and version
#         id: version
#         run: |
#           if [[ "${{ github.ref }}" == refs/tags/v* ]]; then
#             VERSION="${{ github.ref }}"
#             VERSION=${VERSION#refs/tags/}
#             ENVIRONMENT="prod"
#           elif [[ "${{ github.ref }}" == refs/heads/release/* ]]; then
#             ENVIRONMENT="uat"
#             VERSION="${{ github.ref }}"
#             VERSION=${VERSION#refs/heads/release/}
#           elif [[ "${{ github.ref }}" == refs/heads/main ]]; then
#             ENVIRONMENT="staging"
#             VERSION="main-$(date +%s)"
#           elif [[ "${{ github.ref }}" == refs/heads/dev ]]; then
#             ENVIRONMENT="dev"
#             VERSION="dev-$(date +%s)"
#           else
#             ENVIRONMENT="dev"
#             VERSION="pr-${{ github.event.pull_request.number }}"
#           fi
#           echo "environment=$ENVIRONMENT" >> $GITHUB_OUTPUT
#           echo "version=$VERSION" >> $GITHUB_OUTPUT

#       - name: Verify Postman collection exists
#         run: |
#           if [ ! -f "postman/jsonmap.postman_collection.json" ]; then
#             echo "❌ ERROR: Postman collection not found at postman/jsonmap.postman_collection.json"
#             exit 1
#           fi

#       - name: Start mock Central Registry for testing
#         run: |
#           python3 -c "
#           from http.server import HTTPServer, BaseHTTPRequestHandler
#           import json
#           class MockRegistry(BaseHTTPRequestHandler):
#               def do_POST(self):
#                   self.send_response(200)
#                   self.send_header('Content-type', 'application/json')
#                   self.end_headers()
#                   self.wfile.write(json.dumps({'status': 'ok', 'mock': True}).encode())
#               def log_message(self, format, *args):
#                   pass
#           HTTPServer(('', 8085), MockRegistry).serve_forever()
#           " &
#           sleep 3
#           echo "✅ Mock registry running on port 8085"

#       - name: Register API with Central Registry
#         run: |
#           curl -X POST "${{ secrets.CENTRAL_REGISTRY_URL }}/registry/apis/register-file" \
#             -F "apiName=jsonmap-service" \
#             -F "description=JSON Mapping Service" \
#             -F "gitRepository=${{ github.repository }}" \
#             -F "version=${{ steps.version.outputs.version }}" \
#             -F "breakingChange=false" \
#             -F "collectionFile=@postman/jsonmap.postman_collection.json;type=application/json"

#       - name: Register environment
#         run: |
#           curl -X POST "${{ secrets.CENTRAL_REGISTRY_URL }}/registry/environments" \
#             -H "Content-Type: application/json" \
#             -d '{
#               "envName": "${{ steps.version.outputs.environment }}",
#               "baseUrl": "${{ secrets.SERVICE_BASE_URL }}"
#             }' || true

#       - name: Mark API Deployment
#         run: |
#           curl -X POST "${{ secrets.CENTRAL_REGISTRY_URL }}/registry/apis/deploy" \
#             -H "Content-Type: application/json" \
#             -d '{
#               "apiName": "jsonmap-service",
#               "version": "${{ steps.version.outputs.version }}",
#               "environment": "${{ steps.version.outputs.environment }}",
#               "baseUrl": "${{ secrets.SERVICE_BASE_URL }}"
#             }'
name: Register JSON Map Service API

on:
  push:
    branches: [main, dev, "release/**"]
    tags: ["v*"]

jobs:
  register:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Determine environment and version
        id: version
        run: |
          if [[ "${{ github.ref }}" == refs/tags/v* ]]; then
            VERSION="${{ github.ref }}" 
            VERSION=${VERSION#refs/tags/}
            ENVIRONMENT="prod"
          elif [[ "${{ github.ref }}" == refs/heads/release/* ]]; then
            ENVIRONMENT="uat"
            VERSION="${{ github.ref }}"
            VERSION=${VERSION#refs/heads/release/}
          elif [[ "${{ github.ref }}" == refs/heads/main ]]; then
            ENVIRONMENT="staging"
            VERSION="main-$(date +%s)"
          elif [[ "${{ github.ref }}" == refs/heads/dev ]]; then
            ENVIRONMENT="dev"
            VERSION="dev-$(date +%s)"
          else
            ENVIRONMENT="dev"
            VERSION="pr-${{ github.event.pull_request.number }}"
          fi
          echo "environment=$ENVIRONMENT" >> $GITHUB_OUTPUT
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Verify OpenAPI spec exists (optional)
        run: |
          if [ ! -f "openapi.json" ]; then
            echo "⚠️ openapi.json not found – will skip API spec creation"
          fi

      # ---------- Mock Central Registry ----------
      - name: Start mock Central Registry for testing
        run: |
          python3 -c "
          from http.server import HTTPServer, BaseHTTPRequestHandler
          import json
          class MockRegistry(BaseHTTPRequestHandler):
              def do_POST(self):
                  self.send_response(200)
                  self.send_header('Content-type', 'application/json')
                  self.end_headers()
                  self.wfile.write(json.dumps({'status': 'ok', 'mock': True}).encode())
              def log_message(self, format, *args):
                  pass
          HTTPServer(('', 8085), MockRegistry).serve_forever()
          " &
          sleep 3
          echo "✅ Mock registry running on port 8085"

      - name: Register API with Central Registry
        run: |
          curl -X POST "${{ secrets.CENTRAL_REGISTRY_URL }}/registry/apis/register-file" \
            -F "apiName=jsonmap-service" \
            -F "description=JSON Mapping Service" \
            -F "gitRepository=${{ github.repository }}" \
            -F "version=${{ steps.version.outputs.version }}" \
            -F "breakingChange=false" \
            -F "collectionFile=@postman/jsonmap.postman_collection.json;type=application/json"

      - name: Register environment
        run: |
          curl -X POST "${{ secrets.CENTRAL_REGISTRY_URL }}/registry/environments" \
            -H "Content-Type: application/json" \
            -d '{
              "envName": "${{ steps.version.outputs.environment }}",
              "baseUrl": "${{ secrets.SERVICE_BASE_URL }}"
            }' || true

      - name: Mark API Deployment
        run: |
          curl -X POST "${{ secrets.CENTRAL_REGISTRY_URL }}/registry/apis/deploy" \
            -H "Content-Type: application/json" \
            -d '{
              "apiName": "jsonmap-service",
              "version": "${{ steps.version.outputs.version }}",
              "environment": "${{ steps.version.outputs.environment }}",
              "baseUrl": "${{ secrets.SERVICE_BASE_URL }}"
            }'

      # ---------- Postman: Create or Update API SPECIFICATION (under "APIs" tab) ----------
      - name: Create or Update API Specification
        env:
          POSTMAN_API_KEY: ${{ secrets.POSTMAN_API_KEY }}
          POSTMAN_WORKSPACE_ID: ${{ secrets.POSTMAN_WORKSPACE_ID }}
        run: |
          # Use your existing OpenAPI file (rename or adjust path)
          SPEC_FILE="postman/jsonmap.postman_collection.json"   # or "openapi.json"
          if [ ! -f "$SPEC_FILE" ]; then
            echo "⚠️ No OpenAPI spec found – skipping"
            exit 0
          fi

          SPEC_NAME="JSON Map Service API"
          SPEC_VERSION="1.0.0"

          echo "Fetching existing specs in workspace $POSTMAN_WORKSPACE_ID..."
          SPECS=$(curl -s -X GET "https://api.getpostman.com/specs?workspace=$POSTMAN_WORKSPACE_ID" \
            -H "X-Api-Key: $POSTMAN_API_KEY" \
            -H "Accept: application/vnd.api.v10+json")

          SPEC_ID=$(echo "$SPECS" | jq -r --arg NAME "$SPEC_NAME" '.specs[] | select(.name == $NAME) | .id')

          if [ -z "$SPEC_ID" ]; then
            echo "Creating new API specification..."
            curl -X POST "https://api.getpostman.com/specs" \
              -H "X-Api-Key: $POSTMAN_API_KEY" \
              -H "Content-Type: application/json" \
              -H "Accept: application/vnd.api.v10+json" \
              -d '{
                "spec": {
                  "name": "'"$SPEC_NAME"'",
                  "version": "'"$SPEC_VERSION"'",
                  "type": "openapi3_1",
                  "content": '"$(jq -c . "$SPEC_FILE")"'
                }
              }'
          else
            echo "Updating existing API specification (ID: $SPEC_ID)..."
            NEW_VERSION="1.0.$(date +%s)"
            curl -X PUT "https://api.getpostman.com/specs/$SPEC_ID" \
              -H "X-Api-Key: $POSTMAN_API_KEY" \
              -H "Content-Type: application/json" \
              -H "Accept: application/vnd.api.v10+json" \
              -d '{
                "spec": {
                  "name": "'"$SPEC_NAME"'",
                  "version": "'"$NEW_VERSION"'",
                  "type": "openapi3_1",
                  "content": '"$(jq -c . "$SPEC_FILE")"'
                }
              }'
          fi
